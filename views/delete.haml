- if @note
	%p= "Are you sure you want to delete the following note: <em>#{h @note.content}</em>?"
	%form{action: "/#{@note.id}", method: "post"}
		%input{type: "hidden", name: "_method", value: "delete"}
		%input{type: "submit", value: "Dooo it!"}
		%a{href: "/#{@note.id}"} Cancel
- else
	%p Note not found, gurl.
